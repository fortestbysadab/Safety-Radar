// Tests for /api/reports routes — implemented with the backend.
import { describe, it } from 'node:test';

describe('reports routes', () => {
  it('placeholder — backend will ship supertest coverage here', () => {
    // no-op until implementation
  });
});
